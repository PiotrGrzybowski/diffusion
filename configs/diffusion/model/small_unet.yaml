_target_: diffusion.models.unet.Unet
in_channels: ${in_channels}
out_channels: ${out_channels}
init_channels: 8
channels: [8, 16, 32]
downsamples: [True, True, False]
attentions: ["linear", "linear", "dot"]
heads: 4
head_dim: 8
