_target_: diffusion.data.mnist_datamodule.MNISTDataModule
dataset_name: mnist
path: /tmp/data
batch_size: ${batch_size}
num_workers: 0
predict_samples: ${predict_samples}
val_samples_per_label: 4
# shape: [1, 28, 28]
